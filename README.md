# Kwazy Pixels

Kwazy Pixels is a light weight simple API to expose different galleries of images to devices to be displayed as backgrounds, screensavers, etc. It supports requesting images by size and format to support various target devices.